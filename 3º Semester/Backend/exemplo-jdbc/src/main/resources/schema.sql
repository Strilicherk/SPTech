CREATE TABLE IF NOT EXISTS jogo (
    id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    nome VARCHAR(100) NOT NULL,
    genero VARCHAR(150) NOT NULL
);

INSERT INTO jogo (nome, genero)
VALUES ('Minecraft', 'RPG');